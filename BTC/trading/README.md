# BTC / trading

BTC tape (OI, CVD, OBV, funding, basis) and trade log.

Current series: Binance BTCUSDT perp OI (`oi.csv`), funding (`funding.csv`), basis (`basis.csv`), CVD (`cvd_perp.csv`), spot CVD (`cvd_spot.csv`).
