# BTC / scripts

Helpers. One small getter per series. Tool ≠ model.

| Script | Series | Timer |
|---|---|---|
| `get_oi.py` | Binance BTCUSDT perp OI | 10:10 `com.quantblocks.oi.plist` |
| `get_funding.py` | Binance BTCUSDT perp funding | 10:15 `com.quantblocks.funding.plist` |
| `get_basis.py` | Binance BTCUSDT perp basis | 10:20 `com.quantblocks.basis.plist` |

Stdlib only. No API key. Join today’s QMI row unless `--z` is passed.
